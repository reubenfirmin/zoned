kotlin.code.style=official

# CI runners have no user-level gradle.properties; without these the Kotlin
# daemon inherits Gradle's 512m default heap and OOMs during compilation.
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
kotlin.daemon.jvmargs=-Xmx2g